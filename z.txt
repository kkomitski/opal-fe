
PATH=/bin:/usr/bin:/usr/local/bin:/sbin:${PATH}
export PATH

# Homebrew
export PATH=/opt/homebrew/bin:$PATH
export PATH=/opt/homebrew/sbin:$PATH

# VS Code
export PATH="$PATH":"/Applications/Visual Studio Code.app/Contents/Resources/app/bin"

# Fuzzy Finder
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh
export FZF_DEFAULT_OPS="--extended"

# MySQL
export PATH=/usr/local/mysql/bin:$PATH
alias python=/usr/bin/python3

# Java
JAVA_HOME="/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home"
export PATH="${JAVA_HOME}/bin:${PATH}"

# Maven
export M2_HOME="/Library/apache-maven-3.9.5/bin"
export PATH="${M2_HOME}:${PATH}"

# Echo PATH
alias echo-path="echo $PATH | sed -e $'s/:/\\\n/g'"

# Reset terminal
alias restart="clear; exec zsh"

# Add the CodeQL home folder to the PATH
export PATH=$PATH:$HOME/codeql-home/lib/codeql
PATH=~/.console-ninja/.bin:$PATH

# Go
export GOPATH=$HOME/Documents/go-worskpace
export PATH=$PATH:$GOPATH/bin

# pnpm
export PNPM_HOME="/Users/adm-kkomitski/Library/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac
# pnpm end

export DOCKER_HOST=unix://${HOME}/.colima/default/docker.sock
# alias docker='/opt/homebrew/bin/docker'

# Enable Powerlevel10k instant prompt. Should stay close to the top of ~/.zshrc.
# Initialization code that may require console input (password prompts, [y/n]
# confirmations, etc.) must go above this block; everything else may go below.
if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

GITSTATUS_LOG_LEVEL=DEBUG

export NODE_EXTRA_CA_CERTS=/Users/adm-kkomitski/cmc.pem

# If you come from bash you might have to change your $PATH.
# export PATH=$HOME/bin:/usr/local/bin:$PATH

# Path to your oh-my-zsh installation.
export ZSH="$HOME/.oh-my-zsh"

ZSH_THEME="powerlevel10k/powerlevel10k"
# Fix for docker zsh plugin

COMPLETION_WAITING_DOTS="true"

# source $ZSH/oh-my-zsh.sh
plugins=(git web-search zsh-autosuggestions zsh-syntax-highlighting zsh-nvm fzf-zsh-plugin docker docker-compose)

autoload -Uz compinit && compinit # hack to make autocomplete for docker and docker-compose working
source $ZSH/oh-my-zsh.sh

# To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh
source /Users/adm-kkomitski/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh